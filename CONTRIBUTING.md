# 기여 안내

이 프로젝트는 코드 정확성뿐 아니라 출처와 연구 맥락의 정확성을 함께 다룬다.

## 변경 제안

1. 작은 범위의 이슈 또는 제안으로 목적을 설명한다.
2. 외부 데이터를 추가한다면 먼저 소스 매니페스트와 라이선스 근거를 제시한다.
3. 원문, 전사, 번역, 현대 해석, AI 추론을 구분한다.
4. 코드 변경에는 관련 테스트를 추가한다.
5. 문헌 내용 변경에는 확인한 판본·행·페이지·URL을 남긴다.

## 데이터 기여

- 이용 조건이 불명확한 원문·번역·이미지를 커밋하지 않는다.
- 소프트웨어 라이선스를 코퍼스 콘텐츠 라이선스로 간주하지 않는다.
- 대용량 원본은 Git에 넣지 않고 재현 가능한 획득 방법과 체크섬을 기록한다.
- 논쟁적 인물·지명 동일시는 확정 병합이 아니라 출처 있는 후보 주장으로 제출한다.

## 로컬 확인

```bash
PYTHONPATH=src python -m unittest discover -s tests
PYTHONPATH=src python -m ane_context_ai validate examples/context-package.example.json
```

## 커밋

변경 목적이 드러나는 짧은 명령형 제목을 사용한다. 데이터나 연구 판단이 바뀌었다면 본문에 출처와 영향 범위를 설명한다.
